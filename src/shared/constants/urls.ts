export const enum Urls {
  HOME = "/",
  ABOUT = '/about',
  PRODUCTS = "/products",
  GALLERY= "/gallery",
  OUR_PARTNERS = "/partners",
  CERTIFICATES = "/certificate",
  CONTACT = "/contact-us",
}
